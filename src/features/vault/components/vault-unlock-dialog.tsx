// Vault unlock is now handled inline in the Vault page (features/vault/page.tsx)
export function VaultUnlockDialog(_props: { open: boolean; onSuccess: () => void; onClose?: () => void }) {
  return null
}
